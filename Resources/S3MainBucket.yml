Type: AWS::S3::Bucket
Properties:
  BucketName: ${self:custom.s3MainBucket}
  AccessControl: PublicRead
  CorsConfiguration:
    CorsRules:
    - AllowedMethods:
      - GET
      - PUT
      - POST
      - HEAD
      AllowedOrigins:
      - "*"
      AllowedHeaders:
      - "*"
